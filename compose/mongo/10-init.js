// MongoDB initialisation script - runs when MongoDB container starts for the first time
// Add any seed data or initial collection setup here

// Example: create a user
// db.createUser({ user: 'myuser', pwd: 'mypassword', roles: [{ role: 'readWrite', db: 'pafs' }] })

// Example: insert seed data
// db = db.getSiblingDB('pafs')
// db.projects.insertMany([])
